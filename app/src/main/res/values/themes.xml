<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Bars are handled by enableEdgeToEdge() at runtime; the deprecated
         android:statusBarColor / android:windowLightStatusBar attributes
         (deprecated in Android 15 / API 35) are intentionally omitted. -->
    <style name="Theme.NetView" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
